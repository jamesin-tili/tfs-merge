<UserControl x:Class="TFSMergingTool.ConnectionSetup.BranchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TFSMergingTool.ConnectionSetup"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="200">
    <StackPanel Orientation="Horizontal">
        <CheckBox x:Name="IsEnabled" VerticalAlignment="Center" HorizontalAlignment="Center">
            <CheckBox.LayoutTransform>
                <ScaleTransform ScaleX="1.4" ScaleY="1.4" />
            </CheckBox.LayoutTransform>
        </CheckBox>
        <TextBox x:Name="Path" MinWidth="50"/>
    </StackPanel>
</UserControl>
